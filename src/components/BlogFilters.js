import React from 'react';

export default class BlogFilters extends React.Component {
  
  render() {
    return (
      <div>
        Tags, startDate, endDate (always sort by date, but oldest to newest?)
      </div>
    )
  }
}